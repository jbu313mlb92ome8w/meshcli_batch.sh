# Putting "bash " at the beginning of a line executes a bash command.
# All other lines are prefixed with "meshcli -s " or "meshcli -s -r -j " depending on if the device is marked as a repeater or not.

# Placed here in case if a Room Repeater role is selected.
set repeat on

set advert.interval 60
set flood.advert.interval 3

set agc.reset.interval 4
set multi.acks 1
set rxdelay 3
set txdelay 0.3
set direct.txdelay 0.1
set agc.reset.interval 4

powersaving off
gps advert none
gps off
gps

set allow.read.only off
#password (YOUR PASSWORD HERE)
#setperm (YOUR KEY HERE) 3
get prv.key
get public.key

neighbors
